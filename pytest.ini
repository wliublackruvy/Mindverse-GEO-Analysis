[pytest]
minversion = 7.0
testpaths = tests
addopts =
    --strict-markers
    --disable-warnings
    --cov=geo_analyzer
    --cov-report=term-missing
    -q
markers =
    unit: unit tests
    integration: integration tests
    slow: slow tests (explicitly marked)
