[pytest]
minversion = 7.0
testpaths = tests
addopts =
    -q
    --strict-markers
    --disable-warnings
markers =
    unit: unit tests
    integration: integration tests
    slow: slow tests (explicitly marked)
